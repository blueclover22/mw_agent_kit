---
type: Foundation
related_to:
  - "01.overview.md"
  - "03.conventions.md"
  - "04.framework.md"
  - "05.layers.md"
---

# Architecture

> Template: fill in each section for `<project-name>` and delete this notice. This document is a **permanent guide** (no time-stamped status pinning — §00 Content Rules).

Defines the project's common architecture principles as observed in the code. In case of conflict, this document is the top-level authority.

---

## 1. Architecture Overview

_(TODO)_ System composition diagram + layer summary.

## 2. Layers / Dependency Direction

_(TODO)_ Layer definitions and **dependency rules** (e.g., only upper→lower allowed). Specify the architecture in use (layered / hexagonal / FSD / Clean, etc.). How code inside each layer is written is in [`05.layers.md`](05.layers.md).

## 3. State / Data Flow

_(TODO)_ Where state lives (server cache / global / local), data flow cases. Concrete end-to-end traces are in `processes/` (one file per scenario, registered in [`00.INDEX.md`](00.INDEX.md) §Processes) — link, do not repeat them here.

## 4. Module / Directory Structure Principles

_(TODO)_ Slice/module boundaries, public API conventions (`index`, etc.), rules forbidding cross-module dependencies. The actual tree is in [`01.overview.md`](01.overview.md) §3.

## 5. Naming Rules

Naming conventions are managed in [`03.conventions.md`](03.conventions.md) §2 Naming (SSOT). This document keeps only the link.

## 6. Anti-patterns

_(TODO)_ Patterns the code base avoids (and, if observed, where the exceptions are).
