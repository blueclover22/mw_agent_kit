---
type: Operations
related_to:
  - "09.project-guide.md"
  - "11.test-guide.md" # standard-only
---

# Build & Operations Guide

> Template: replace the build tooling to match the actual stack (web: npm/pnpm · Tauri: cargo+pnpm · Android: Gradle). Delete this notice. Permanent guide (operator-guide-style checklists are allowed — §00 Content Rules).

Defines the build and verification pipeline, run/debug, and operations (logs, incident response). The environment variable key list is in [`09.project-guide.md`](09.project-guide.md) §Environment Variables.

---

## 1. Build System Overview
_(TODO)_ Build tools, artifacts, targets (web bundle / Tauri app / apk·aab).

## 2. Build Commands
_(TODO)_

| Step | Command |
| :--- | :--- |
| Install dependencies | `<cmd>` |
| Run in development | `<cmd>` |
| Production build | `<cmd>` |

## 3. Bundling / Packaging
_(TODO)_ Bundler, signing, distribution artifacts (web static/server · Tauri installer · Android aab).

## 4. Verification Pipeline
_(TODO)_ Type check → lint → tests ([`11.test-guide.md`](11.test-guide.md)) → build order and each command. _(standard-only)_

- _(profile — compact: replace the line above with the same sentence minus the `11` link, i.e. "_(TODO)_ Type check → lint → tests → build order and each command." — test execution lives in §Tests below.)_

### Tests

_(profile — standard: the test SSOT is 11; this subsection does not exist pre-fold — delete it entirely (heading included) during cleanup. compact: 11 isn't part of this profile, so keep this subsection as the permanent home for test execution and conventions; delete only this note once filled.)_

_(TODO)_ Test layer classification (unit / integration / e2e), placement rules, execution commands, case naming, coverage policy.

## 5. CI / CD Entry Points
_(TODO)_ Pipeline triggers, stages, secret injection points.

## 6. Run / Debug
_(TODO)_ Local run, debugger attachment, hot reload, source map setup.

## 7. Operations / Logs
_(TODO)_ Runtime operations procedures.

- **Log location / format**: _(TODO)_ Per-OS paths, file naming rules, line format.
- **Collection procedure**: _(TODO)_ Collection and delivery steps, pre-check for sensitive data.
- **Incident response checklist**: _(TODO)_ Diagnosis and response per symptom.
- **Retention / cleanup policy**: _(TODO)_ Retention period, automatic cleanup.
