---
type: DomainGuide
related_to:
  - "01.Glossary.md" # standard-only
  - "02.architecture.md"
  - "04.framework-guide.md"
  - "08.domains.md" # standard-only
  - "12.process-guide.md" # standard-only
---

# Frontend Guide

_(profile — standard: this document covers only the UI layer; core/backend conventions live in 07. compact: 07 isn't part of this profile, so this document also covers the core/data layer via §7 below; the H1 becomes `Application Guide` and the file name stays `06.frontend-guide.md`.)_

> Template: if not applicable, delete this document and adjust the §00 table of contents and numbering. Permanent guide.

Defines state, data, and styling conventions for the Frontend / UI layer.

---

## 1. State Layer Conventions

_(TODO)_ Which state lives in which store (server cache / global / screen-local).

## 2. Component Structure

_(TODO)_ Component classification and composition rules, segment mapping.

## 3. Data Fetching / Cache

_(TODO)_ Server communication boundaries, cache key conventions, invalidation policy.

## 4. Routing

_(TODO)_ Route definitions, guards, loading/error boundaries.

## 5. Styling

_(TODO)_ Style tokens and theme conventions.

## 6. Segment Mapping

_(TODO)_ Directory ↔ responsibility mapping (consistent with [`02.architecture.md`](02.architecture.md)).

## 7. Core / Data Layer

_(profile — standard: layer structure / service conventions / persistence / external integrations / errors·transactions are managed in 07; this section does not exist pre-fold — delete it entirely (heading included) during cleanup. compact: 07 isn't part of this profile, so keep this section as the permanent home for the core/data layer; delete only this note once filled.)_

_(TODO)_ Layers such as service / repository and their responsibility boundaries; business logic module conventions; data access patterns, transactions, migrations; HTTP client / messaging / external API integration conventions; error classification and propagation, transaction boundaries.
