---
type: Foundation
related_to:
  - "01.overview.md"
  - "02.architecture.md"
  - "03.conventions.md"
  - "05.layers.md"
---

# `<framework>` Guide

> Template: replace **`<framework>` in the title (H1)** above with the actual stack name (Spring Boot · web: React / Next.js · Tauri · Android). **Do not change the file name `04.framework.md`** (fixed regardless of stack). Delete this notice. Permanent guide.

Defines the common mechanisms of the key framework/runtime as the project uses them, and holds the **external interface contract catalog** (§5) — the SSOT for API endpoints / commands / Intents.

---

## 1. Overview

_(TODO)_ Versions, summary of core concepts.

## 2. Core Mechanisms

_(TODO)_ Core framework features and project usage conventions — DI / routing / events·lifecycle / IPC / middleware, etc.

## 3. Error Handling Mechanisms

_(TODO)_ Framework-level error handling and propagation (general policy in [`03.conventions.md`](03.conventions.md) §4).

## 4. Configuration / Environment

_(TODO)_ Configuration loading · profiles · environment branching mechanisms (environment variable key list in [`01.overview.md`](01.overview.md) §4, build-time injection in [`06.build-ops.md`](06.build-ops.md)).

## 5. Contract Catalog

**SSOT for external interface contracts** (Spring Boot: REST endpoints · web: REST / API routes · Tauri: IPC commands · Android: Intents · screen contracts). Other documents link here.

### 5.1 Common Rules

_(TODO)_ Request/response envelope, common error codes, auth headers, serialization conventions.

### 5.2 Interface List

_(TODO)_ Interface list, grouped per domain when the project has `domains/`.

| Name | Input | Output | Description | Definition location |
| :--- | :--- | :--- | :--- | :-------- |
| `<name>` | `<In>` | `<Out>` | ... | `<path>` |

### 5.3 Change Procedure

_(TODO)_ Procedure for synchronizing call sites and documents when a contract changes.
