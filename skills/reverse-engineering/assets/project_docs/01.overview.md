---
type: Overview
related_to:
  - "02.architecture.md"
  - "04.framework.md"
  - "06.build-ops.md"
---

# Project Overview (Fact Sheet)

> Template: a fact sheet containing **only project-specific information that has no home in other documents**. Conventions themselves live in the Foundation and Layer documents; link to them from here (§00 Per-Topic SSOT). Fill in the `<...>` placeholders and delete this notice. Permanent guide.

Connects the conventions of the other documents to what and where they apply in this project. When conventions conflict, Foundation documents take precedence.

---

## 1. Project Scope
_(TODO)_ Project purpose, feature scope, boundaries (what it does and does not do).

## 2. Tech Stack
_(TODO)_ Project-specific libraries/tools and versions (from the dependency files — `package.json` / `Cargo.toml` / `build.gradle` / …).

## 3. Current Structure
_(TODO)_ Folder/file structure tree + segment ↔ responsibility mapping, and the **entry points** (main / bootstrap / route root / command registry) that `processes/` traces start from. Architecture principles live in [`02.architecture.md`](02.architecture.md).

```
<repo>/
  ├─ ...
  └─ ...
```

| Entry point | Path | Starts |
| :--- | :--- | :--- |
| `<name>` | `<path>` | `<what it bootstraps>` |

## 4. Environment Variables
_(TODO)_ **SSOT for the environment variable key list and value policy.** `.env` structure, precedence, required/optional keys. Loading mechanism is in [`04.framework.md`](04.framework.md) §4; build-time injection is in [`06.build-ops.md`](06.build-ops.md).

| Key | Required | Purpose | Default/Example |
| :--- | :--- | :--- | :--- |
| `<KEY>` | ✓ / ✗ | ... | `<value>` |

## 5. Change Checklist
_(TODO)_ Work order for adding/modifying a feature and **which documents to update**.

1. Check conventions — the relevant layer in [`05.layers.md`](05.layers.md), framework mechanisms in [`04.framework.md`](04.framework.md), and the feature's `domains/<name>.md` if one exists
2. Implement — follow [`03.conventions.md`](03.conventions.md)
3. When changing external interfaces, update [`04.framework.md`](04.framework.md) §5 Contract Catalog
4. Verify — [`06.build-ops.md`](06.build-ops.md) Verification Pipeline
5. When a new domain or scenario appeared, add `domains/<name>.md` / `processes/<name>.md` and register it in [`00.INDEX.md`](00.INDEX.md)

## 6. Troubleshooting
_(TODO)_ Project-specific errors and resolution procedures (general build/operations issues go to [`06.build-ops.md`](06.build-ops.md)).
