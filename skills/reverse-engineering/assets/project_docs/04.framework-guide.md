---
type: Foundation
related_to:
  - "02.architecture.md"
  - "03.coding-rules.md"
  - "05.framework-api.md" # standard-only
  - "06.frontend-guide.md"
  - "07.backend-guide.md" # standard-only
  - "09.project-guide.md"
---

# `<framework>` Guide

> Template: replace **`<framework>` in the title (H1)** above with the actual stack name (Spring Boot · web: React / Next.js · Tauri · Android). **Do not change the file name `04.framework-guide.md`** (fixed regardless of stack). Delete this notice. Permanent guide.

Defines the common mechanisms of the key framework/runtime. The contract list is in [`05.framework-api.md`](05.framework-api.md). _(standard-only)_

---

## 1. Overview

_(TODO)_ Versions, summary of core concepts.

## 2. Core Mechanisms

_(TODO)_ Core framework features and project usage conventions — DI / routing / events·lifecycle / IPC / middleware, etc.

## 3. Error Handling Mechanisms

_(TODO)_ Framework-level error handling and propagation.

## 4. Configuration / Environment

_(TODO)_ Configuration loading · profiles · environment branching mechanisms (environment variable key list in [`09.project-guide.md`](09.project-guide.md), build-time injection in [`10.build-ops-guide.md`](10.build-ops-guide.md)).

## 5. Contract Catalog Link

_(profile — standard: the contract catalog SSOT is 05. Keep this heading and the `_(TODO)_ External interfaces …` line exactly as written below, and delete everything after it in this section — the `_(TODO)_ Interface list` line and the table — since neither exists pre-fold. compact: 05 isn't part of this profile — rename this heading to `Contract Catalog`, delete the `_(TODO)_ External interfaces …` line instead, and record the interface list directly in the table. Delete this note in both profiles once done.)_

_(TODO)_ External interfaces (API endpoints / commands / Intents, etc.) are managed in [`05.framework-api.md`](05.framework-api.md). _(standard-only)_

_(TODO)_ Interface list (name · input · output · description · definition location).

| Name | Input | Output | Description | Definition location |
| :--- | :--- | :--- | :--- | :-------- |
| `<name>` | `<In>` | `<Out>` | ... | `<path>` |
